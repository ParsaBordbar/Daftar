# دفتر — صفحه‌ساز شعر فارسی

[English](README.md) · **فارسی**

«دفتر» یک نسخه‌ی رایگان و بدون بک‌اند از [The Booklet](https://the-booklet.app) برای ساخت صفحه‌های زیبا از شعر و نثر فارسی است.

کافی است شعر موردنظرتان را بنویسید یا وارد کنید، یک قلم فارسی و طرح کاغذ انتخاب کنید و در پایان، صفحه‌ی آماده‌شده را در اندازه‌ی دلخواهتان به‌صورت تصویر PNG با کیفیت بالا خروجی بگیرید.

همه‌چیز مستقیماً داخل مرورگر اجرا می‌شود؛ نه سروری در کار است و نه نیازی به ساخت حساب دارید. متن شعرتان هم هیچ‌وقت از صفحه خارج نمی‌شود.

تنها استثنا، جست‌وجوی کامل در گنجور است: عبارتی که جست‌وجو می‌کنید برای پیدا کردن شعرهای مرتبط به API عمومی گنجور فرستاده می‌شود. با این حال، مرور و خواندن شعرها مستقیماً از نسخه‌ی عمومی `ganjoor-data` روی jsDelivr انجام می‌شود.

## چه کارهایی می‌توانید با دفتر انجام دهید؟

* **تقریباً تمام گنجینه‌ی شعر فارسی، همین‌جا در دسترس شماست.** می‌توانید میان آثار [گنجور](https://ganjoor.net)، شامل ۲۳۴ شاعر و حدود ۱۳۲ هزار شعر، بگردید؛ شعر موردنظرتان را انتخاب کنید، فقط بیت‌هایی را که دوست دارید نگه دارید و آن را همراه با عنوان، نام شاعر و منبع، مستقیماً روی صفحه بیاورید. اندازه‌ی متن هم به‌صورت خودکار تنظیم می‌شود تا شعر به‌خوبی داخل صفحه جا بگیرد.

* **جست‌وجوی کامل در متن شعرها.** اگر فقط بخشی از یک مصرع را به یاد دارید، همان را بنویسید تا شعرهایی که آن عبارت را دارند پیدا شوند. می‌توانید جست‌وجو را به شاعر فعلی محدود کنید یا در تمام گنجور بگردید. بخش‌های منطبق هم در نتیجه‌ها مشخص می‌شوند.

* **۱۸ قلم فارسی در چهار گروه مختلف:** نستعلیق، نسخ، بی‌سریف و تزئینی. همه‌ی قلم‌ها رایگان و دارای مجوز آزاد هستند. قلم‌ها فقط هنگام نیاز بارگذاری می‌شوند؛ بنابراین با ورود به سایت لازم نیست هر ۱۸ قلم را یک‌جا دانلود کنید.

* **۸ طرح کاغذ متفاوت:** کاغذ کهنه، شب، لاجوردی، فیروزه‌ای، زعفرانی، سفید، اناری و زغالی. هر طرح رنگ مخصوص خودش را برای متن، جزئیات تزئینی و تذهیب دارد.

* **سه مدل چینش شعر:** یک مصرع در هر خط، نمایش دو مصرع کنار هم برای بیت‌های کلاسیک و چینش آزاد برای شعر نو.

* **تزئینات الهام‌گرفته از نسخه‌های خطی:** جداکننده، قاب دوتایی، نقش‌های گوشه‌ای و شمسه.

* **۱۳ اندازه‌ی آماده برای خروجی:** استوری، پست مربعی و پست عمودی اینستاگرام، پینترست، لینکدین، X، تلگرام، واتساپ، کاغذهای A4 و A5 با کیفیت ۳۰۰ DPI و همچنین تصویر پس‌زمینه‌ی موبایل و دسکتاپ. هرکدام را هم می‌توانید با کیفیت 1×، 2× یا 3× خروجی بگیرید.

* **لینک اشتراک‌گذاری قابل ویرایش.** تمام اطلاعات صفحه داخل بخش Fragment آدرس قرار می‌گیرد و چیزی در جایی ذخیره نمی‌شود. هرکس لینک را باز کند، دقیقاً همان صفحه‌ی شما را می‌بیند و می‌تواند آن را ویرایش کند.

* **شمارنده‌ی ناشناس صفحات ساخته‌شده.** این شمارنده فقط یک عدد را افزایش می‌دهد و هیچ اطلاعات دیگری ثبت نمی‌کند.

## شعرها از کجا می‌آیند؟

مخزن [`ganjoor/ganjoor-data`](https://github.com/ganjoor/ganjoor-data)، مجموعه‌ی گنجور را به‌صورت فایل‌های JSON منتشر می‌کند. این فایل‌ها از طریق jsDelivr و با دسترسی CORS آزاد ارائه می‌شوند؛ بنابراین برنامه می‌تواند بدون داشتن بک‌اند، اطلاعات را مستقیماً از CDN دریافت کند.

فایل `src/lib/ganjoor.ts` سه نوع فایل را دریافت می‌کند:

* فایل `manifest.json` برای فهرست شاعران
* فایل `_cat.json` برای کتاب‌ها و فصل‌ها
* یک فایل جداگانه برای هر شعر

تمام این فایل‌ها پس از دریافت، هم در حافظه و هم در `sessionStorage` مرورگر کش می‌شوند.

گنجور هر بیت را به‌شکل دو مصرع با `CoupletIndex` یکسان ذخیره می‌کند. مصرع اول با `Right` و مصرع دوم با `Left` مشخص می‌شود. تابع `toCouplets` این ساختار را به مدل بندهای خود برنامه تبدیل می‌کند و مصرع‌هایی را که با نوع `Comment` مشخص شده‌اند کنار می‌گذارد.

به‌صورت پیش‌فرض، برنامه آخرین نسخه‌ی شاخه‌ی `@main` را دنبال می‌کند. اگر می‌خواهید همیشه از یک نسخه‌ی ثابت استفاده شود، مقدار `VITE_GANJOOR_REF` را برابر SHA یک کامیت مشخص قرار دهید.

> نکته: مخزن `ganjoor-data` در حال حاضر مجوز مشخصی اعلام نکرده است. خود شعرهای کلاسیک مدت‌هاست که خارج از محدوده‌ی کپی‌رایت قرار دارند و این برنامه هم داده‌ها را هنگام اجرا می‌خواند و آن‌ها را دوباره منتشر نمی‌کند. با این حال، اگر قصد دارید نسخه‌ای از داده‌ها را مستقیماً داخل مخزن خودتان قرار دهید، بهتر است ابتدا موضوع مجوز را با نگه‌دارندگان گنجور هماهنگ کنید.

## اجرای پروژه

```bash
npm install
npm run dev
```

## انتشار روی GitHub Pages

کافی است تغییراتتان را به شاخه‌ی `main` پوش کنید. فایل `.github/workflows/deploy.yml` پروژه را می‌سازد و منتشر می‌کند و مقدار `base` در Vite را نیز به‌صورت خودکار از نام مخزن می‌گیرد.

فقط برای بار اول، در تنظیمات مخزن این مسیر را فعال کنید:

**Settings → Pages → Source: GitHub Actions**

اگر می‌خواهید پروژه را مستقیماً از سیستم خودتان منتشر کنید، از دستور زیر استفاده کنید:

```bash
VITE_BASE=/<repo-name>/ npm run deploy
```

## مجوز قلم‌ها

تمام قلم‌های موجود در بخش انتخاب فونت رایگان و دارای مجوز آزاد هستند. شانزده قلم از این هجده قلم با مجوز SIL OFL 1.1 منتشر شده‌اند و هنگام اجرا از Google Fonts دریافت می‌شوند؛ بنابراین این مخزن آن‌ها را بازنشر نمی‌کند. پیش از افزوده‌شدن هر قلم، وجود حرف‌های فارسی پ چ ژ گ ک ی در آن بررسی شده است.

دو قلم هم همراه پروژه در پوشه‌ی [`public/fonts/`](public/fonts/README.md) قرار دارند: **میخک** با مجوز SIL OFL 1.1 و **تنها** با مجوز Bitstream Vera که تغییرهای طراح آن در مالکیت عمومی قرار دارد. هر دو مجوز الزام می‌کنند که متن مجوز همراه خود قلم منتشر شود؛ بنابراین فایل‌های `Mikhak-OFL.txt` و `Tanha-LICENSE.txt` باید در همان پوشه باقی بمانند.

ده قلمی که پیش‌تر همراه پروژه بودند حذف شدند، چون یا کپی‌رایت را اعلام کرده بودند بدون آنکه اجازه‌ی استفاده بدهند، یا صراحتاً «تمام حقوق محفوظ است» را ذکر کرده بودند. فهرست کامل و دلیل حذف هرکدام در بخش Removed فایل [`public/fonts/README.md`](public/fonts/README.md) آمده است.

فهرست قلم‌های بخش انتخاب فونت مستقیماً از آرایه‌ی `FONTS` در `src/lib/fonts.ts` ساخته می‌شود؛ بنابراین افزودن یا حذف یک قلم فقط یک تغییر تک‌فایلی است.

## تنظیمات پروژه

| مورد                                            | محل تنظیم                                                      |
| ----------------------------------------------- | -------------------------------------------------------------- |
| نام محصول، آدرس، متن واترمارک و نوشته‌های فارسی | `src/lib/brand.ts`                                             |
| فهرست قلم‌ها                                    | `src/lib/fonts.ts`                                             |
| طرح‌های کاغذ                                    | `src/lib/themes.ts`                                            |
| اندازه‌های خروجی                                | `src/lib/formats.ts`                                           |
| آدرس شمارنده                                    | `src/lib/counter.ts` یا `VITE_COUNTER_NS` و `VITE_COUNTER_KEY` |
| نسخه‌ی داده‌های گنجور                           | `VITE_GANJOOR_REF` با مقدار پیش‌فرض `main`                     |

شمارنده به‌صورت پیش‌فرض از یک سرویس عمومی رایگان استفاده می‌کند. اگر این سرویس در دسترس نباشد، مسدود شود یا با محدودیت درخواست روبه‌رو شود، برنامه به‌طور خودکار از یک شمارنده‌ی محلی استفاده می‌کند و بدون مشکل به کارش ادامه می‌دهد.

اگر هم بخواهید سرویس شمارنده را عوض کنید، فقط کافی است یک فایل را تغییر دهید و بخش‌های دیگر برنامه تحت تأثیر قرار نمی‌گیرند.

## چطور خروجی دقیقاً شبیه پیش‌نمایش می‌ماند؟

کتابخانه‌ی `html-to-image` فقط زمانی می‌تواند قلم‌ها را داخل تصویر قرار دهد که به فایل آن‌ها دسترسی داشته باشد. این کتابخانه نمی‌تواند قوانین یک استایل‌شیت خارجی را از دامنه‌ای دیگر بخواند.

برای حل این مشکل، تمام قلم‌ها—چه قلم‌های همراه پروژه و چه قلم‌های دریافت‌شده از Google Fonts—ابتدا دانلود می‌شوند، به Base64 تبدیل می‌شوند و سپس در زمان اجرا به‌صورت یک `@font-face` با Data URL به صفحه اضافه می‌شوند. این فرایند در فایل `src/lib/fonts.ts` انجام می‌شود.

CSS تولیدشده از طریق گزینه‌ی `fontEmbedCSS` به `html-to-image` داده می‌شود. به این ترتیب:

* تصویر خروجی دقیقاً با پیش‌نمایش یکسان می‌ماند.
* هنگام هر خروجی گرفتن، نیازی به بررسی دوباره‌ی تمام استایل‌شیت‌ها نیست.

خود صفحه همیشه با اندازه‌ی واقعی‌اش—برای مثال ۱۰۸۰×۱۹۲۰ پیکسل—داخل DOM قرار دارد و فقط برای نمایش در بخش پیش‌نمایش با CSS کوچک می‌شود. بنابراین فایل نهایی یک ثبت دقیق و پیکسل‌به‌پیکسل از همان صفحه است، نه نسخه‌ای که دوباره با اندازه‌ای دیگر رندر شده باشد.

## مجوز پروژه

کد این پروژه با مجوز [MIT](LICENSE) منتشر شده است. می‌توانید از آن استفاده کنید، فورک بگیرید، تغییرش دهید و حتی به‌صورت تجاری منتشرش کنید؛ بدون نیاز به اجازه و بدون هیچ هزینه‌ای. تنها شرطی که MIT می‌گذارد این است که خط کپی‌رایت و متن مجوز همراه سورس باقی بماند.

فراتر از آن، یک اشاره‌ی ساده کافی است. مثلاً نوشتن «بر پایه‌ی [دفتر](https://github.com/ParsaBordbar/Daftar)» در README، صفحه‌ی درباره یا فوتر پروژه‌تان دقیقاً همان چیزی است که از این مجوز انتظار می‌رود. این یک درخواست است، نه یک الزام؛ مجوز پروژه MIT است و هیچ شرطی به آن اضافه نشده است.

مجوز MIT فقط کد و محتوای خود این مخزن را پوشش می‌دهد و مواردی را که با مجوز مستقل خودشان می‌آیند شامل نمی‌شود:

* **قلم‌های همراه پروژه** — میخک با SIL OFL 1.1 و تنها با Bitstream Vera. متن مجوز هر دو باید همراه قلم بماند، پس فایل‌های `public/fonts/Mikhak-OFL.txt` و `public/fonts/Tanha-LICENSE.txt` را در فورک یا بیلد خود نگه دارید.
* **قلم‌های Google Fonts** — با مجوز SIL OFL 1.1، هنگام اجرا دریافت می‌شوند و هرگز توسط این مخزن بازنشر نمی‌شوند.
* **شعرها** — مجموعه‌ی گنجور که هنگام اجرا از CDN خوانده می‌شود. خود متن‌ها کلاسیک و خارج از کپی‌رایت هستند، اما مخزن `ganjoor-data` مجوزی اعلام نکرده است؛ پیش از قرار دادن نسخه‌ای از داده‌ها داخل مخزن خودتان، بخش «شعرها از کجا می‌آیند؟» را بخوانید.
