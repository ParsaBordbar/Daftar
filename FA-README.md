<div align="center">

# دفتر · Daftar

**صفحه‌ساز شعر فارسی؛ ساده، رایگان و کاملاً داخل مرورگر شما.**

[**▶ باز کردن دفتر**](https://parsabordbar.github.io/Daftar/) · [گزارش مشکل](https://github.com/ParsaBordbar/Daftar/issues) · [English](README.md)

[![Deploy](https://github.com/ParsaBordbar/Daftar/actions/workflows/deploy.yml/badge.svg)](https://github.com/ParsaBordbar/Daftar/actions/workflows/deploy.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![React 19](https://img.shields.io/badge/React-19-61dafb?logo=react\&logoColor=white)](https://react.dev)
[![TypeScript](https://img.shields.io/badge/TypeScript-3178c6?logo=typescript\&logoColor=white)](https://www.typescriptlang.org)
[![Vite](https://img.shields.io/badge/Vite-8-646cff?logo=vite\&logoColor=white)](https://vite.dev)
[![Tailwind CSS](https://img.shields.io/badge/Tailwind-4-38bdf8?logo=tailwindcss\&logoColor=white)](https://tailwindcss.com)
![No backend](https://img.shields.io/badge/backend-none-brightgreen)
[![Poems: Ganjoor](https://img.shields.io/badge/poems-Ganjoor-8b5e34)](https://ganjoor.net)

<p>
  <img src="docs/page-kaghaz.png"  width="30%" alt="نی‌نامه‌ی مولانا روی کاغذ کهنه با قلم نستعلیق" />
  <img src="docs/page-shab.png"    width="30%" alt="همان شعر روی طرح شب با قلم امیری" />
  <img src="docs/page-zaferan.png" width="30%" alt="همان شعر روی طرح زعفران با قلم گلزار" />
</p>

<sub>نی‌نامه‌ی مولانا در اندازه‌ی استوری — کاغذ کهنه / نستعلیق · شب / نسخ · زعفران / گلزار</sub>

</div>

---

## دفتر چیست؟

«دفتر» ابزاری برای ساختن صفحه‌های زیبا از شعر فارسی است.

شعرتان را خودتان بنویسید، جایی کپی کنید یا مستقیماً از گنجینه‌ی گنجور انتخابش کنید. بعد قلم، طرح کاغذ و چیدمان دلخواهتان را انتخاب کنید و در نهایت، صفحه‌ی آماده را با کیفیت بالا و در اندازه‌ی مناسب برای استوری، پست، تلگرام، چاپ یا هر جای دیگری که می‌خواهید، به‌صورت PNG تحویل بگیرید.

همه‌چیز داخل مرورگر شما انجام می‌شود؛ نه نیازی به ساخت حساب دارید، نه متن شعرتان برای پردازش روی سروری فرستاده می‌شود و نه لازم است برای ساخت یک صفحه‌ی شعر سراغ چند ابزار مختلف بروید.

«دفتر» یک برنامه‌ی کاملاً ایستاست که روی GitHub Pages اجرا می‌شود.

> **همین حالا امتحانش کنید ← https://parsabordbar.github.io/Daftar/**

## قابلیت‌ها

### تقریباً تمام گنجینه‌ی شعر فارسی، همین‌جا

در «دفتر» به حدود ۲۳۴ شاعر و نزدیک به ۱۳۲٬۰۰۰ شعر از گنجور دسترسی دارید.

می‌توانید شاعر و شعر موردنظرتان را پیدا کنید، فقط بیت‌هایی را که می‌خواهید نگه دارید و بعد شعر را همراه با عنوان، نام شاعر و منبع مستقیماً روی صفحه بیاورید. اندازه‌ی نوشته هم به‌صورت خودکار با محتوایی که انتخاب کرده‌اید هماهنگ می‌شود.

حتی اگر فقط بخشی از یک مصرع یادتان مانده باشد، همان چند کلمه کافی است. جست‌وجوی تمام‌متن «دفتر» شعرهای مرتبط را پیدا می‌کند؛ چه بخواهید فقط میان آثار یک شاعر بگردید و چه در تمام گنجور. بخش منطبق با عبارت جست‌وجوشده هم در نتایج مشخص می‌شود.

### ۱۸ قلم فارسی

۱۸ قلم فارسی در چهار گروه نستعلیق، نسخ، بی‌سریف و تزئینی در دسترس است.

همه‌ی قلم‌ها رایگان و دارای مجوز آزاد هستند و پیش از اضافه شدن، پشتیبانی آن‌ها از حروف فارسی «پ، چ، ژ، گ، ک و ی» بررسی شده است.

قلم‌ها فقط زمانی بارگذاری می‌شوند که به آن‌ها نیاز داشته باشید؛ بنابراین با هر بار باز کردن «دفتر» قرار نیست هر ۱۸ قلم را دانلود کنید.

### ۸ طرح کاغذ

هشت طرح با حال‌وهوای متفاوت در اختیار دارید:

`کاغذ` کهنه · `شب` · `لاجورد` · `فیروزه` · `زعفران` · `سپید` · `انار` · `زغال`

هر طرح رنگ مرکب، رنگ تأکید و رنگ تذهیب مخصوص خودش را دارد تا ظاهر صفحه فقط به تغییر رنگ پس‌زمینه محدود نباشد.

### چیدمان و تزئین

برای چیدمان شعر سه حالت دارید:

* یک مصرع در هر خط
* دو مصرع کنار هم برای بیت‌های کلاسیک
* چیدمان آزاد برای شعر نو

در کنار آن‌ها می‌توانید از تزئیناتی الهام‌گرفته از نسخه‌های خطی فارسی استفاده کنید؛ از جداکننده و قاب دوتایی گرفته تا نقش‌های گوشه‌ای و شمسه.

### ۱۳ اندازه‌ی خروجی، با کیفیت ۱× / ۲× / ۳×

«دفتر» برای اندازه‌های رایج شبکه‌های اجتماعی، نمایشگر و چاپ، خروجی آماده دارد:

| اندازه               | پیکسل در ۱× | اندازه            | پیکسل در ۱× |
| -------------------- | ----------- | ----------------- | ----------- |
| استوری اینستاگرام    | ۱۰۸۰ × ۱۹۲۰ | تلگرام            | ۱۲۸۰ × ۱۶۰۰ |
| پست مربع اینستاگرام  | ۱۰۸۰ × ۱۰۸۰ | چاپی A4           | ۱۲۴۰ × ۱۷۵۴ |
| پست عمودی اینستاگرام | ۱۰۸۰ × ۱۳۵۰ | چاپی A5           | ۸۷۴ × ۱۲۴۰  |
| پینترست              | ۱۰۰۰ × ۱۵۰۰ | پس‌زمینه‌ی موبایل | ۱۴۴۰ × ۳۱۲۰ |
| پینترست بلند         | ۱۰۰۰ × ۲۱۰۰ | پس‌زمینه‌ی دسکتاپ | ۲۵۶۰ × ۱۴۴۰ |
| لینکدین              | ۱۲۰۰ × ۶۲۸  |                   |             |
| لینکدین مربع         | ۱۲۰۰ × ۱۲۰۰ |                   |             |
| ایکس / توییتر        | ۱۶۰۰ × ۹۰۰  |                   |             |

اندازه‌های A4 و A5 در حالت ۱× معادل ۱۵۰ DPI هستند. اگر خروجی مناسب چاپ واقعی با **۳۰۰ DPI** می‌خواهید، کافی است آن‌ها را با کیفیت ۲× بگیرید:

* A4 → `۲۴۸۰ × ۳۵۰۸`
* A5 → `۱۷۴۸ × ۲۴۸۰`

### لینکی که صفحه‌ی شما را با خودش می‌برد

برای به‌اشتراک‌گذاشتن صفحه لازم نیست تصویر یا اطلاعات آن جایی ذخیره شود.

تمام وضعیت صفحه فشرده می‌شود و داخل بخش `fragment` آدرس قرار می‌گیرد. در نتیجه هرکس لینک را باز کند، دقیقاً همان صفحه‌ای را می‌بیند که شما ساخته‌اید و حتی می‌تواند نسخه‌ی خودش را از روی آن ویرایش کند.

هیچ‌کدام از این اطلاعات برای ذخیره‌سازی به سرور فرستاده نمی‌شوند.

## حریم خصوصی؛ چه چیزی از مرورگر شما خارج می‌شود؟

«دفتر» طوری ساخته شده که تا جای ممکن همه‌چیز روی دستگاه خودتان انجام شود.

| مورد                                     | از دستگاه شما خارج می‌شود؟                                                                                |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| متن شعر، عنوان، چیدمان و تصویر PNG خروجی | **نه.** همه‌چیز به‌صورت محلی پردازش و ساخته می‌شود و خروجی هیچ‌وقت آپلود نمی‌شود.                         |
| لینک اشتراک‌گذاری                        | **نه.** وضعیت صفحه داخل `fragment` آدرس قرار دارد و مرورگرها `fragment` را برای سرور ارسال نمی‌کنند.      |
| مرور و خواندن شعرهای گنجور               | **بله.** فایل‌ها مستقیماً از نسخه‌ی `ganjoor-data` روی jsDelivr خوانده می‌شوند.                           |
| جست‌وجوی تمام‌متن گنجور                  | **بله.** عبارت جست‌وجو برای پیدا کردن شعرهای مرتبط به API عمومی گنجور (`api.ganjoor.net`) فرستاده می‌شود. |
| شمارنده‌ی صفحات                          | فقط یک افزایش ساده به یک سرویس شمارنده‌ی عمومی فرستاده می‌شود؛ بدون محتوا و بدون شناسه.                   |

## اجرای محلی

برای اجرای «دفتر» روی سیستم خودتان:

```bash
git clone https://github.com/ParsaBordbar/Daftar.git
cd Daftar
npm install
npm run dev
```

دستورهای اصلی پروژه:

| دستور             | کاربرد                                                      |
| ----------------- | ----------------------------------------------------------- |
| `npm run dev`     | اجرای سرور توسعه‌ی Vite همراه با HMR                        |
| `npm run build`   | بررسی typeها با `tsc -b` و سپس ساخت نسخه‌ی نهایی در `dist/` |
| `npm run preview` | اجرای محلی نسخه‌ی production                                |
| `npm run lint`    | اجرای [oxlint](https://oxc.rs)                              |
| `npm run deploy`  | ساخت و انتشار `dist/` روی `gh-pages` از روی سیستم خودتان    |

## انتشار

با هر `push` روی شاخه‌ی `main`، فایل [`.github/workflows/deploy.yml`](.github/workflows/deploy.yml) اجرا می‌شود.

این workflow پروژه را با:

```text
VITE_BASE=/<repo-name>/
```

می‌سازد، `index.html` را به `404.html` کپی می‌کند تا لینک‌های مستقیم و عمیق هم درست کار کنند و در نهایت نسخه‌ی ساخته‌شده را روی GitHub Pages منتشر می‌کند.

فقط بار اول لازم است در تنظیمات مخزن، GitHub Actions را به‌عنوان منبع Pages انتخاب کنید:

**Settings → Pages → Source: GitHub Actions**

اگر می‌خواهید پروژه را مستقیماً از روی سیستم خودتان منتشر کنید، حتماً مقدار `VITE_BASE` را مشخص کنید.

مقدار پیش‌فرض در `vite.config.ts` برابر با `/daftar/` و با حرف کوچک است، درحالی‌که مسیرهای GitHub Pages به بزرگی و کوچکی حروف حساس‌اند:

```bash
VITE_BASE=/Daftar/ npm run deploy
```

## دفتر چطور کار می‌کند؟

### شعرها از کجا می‌آیند؟

مخزن [`ganjoor/ganjoor-data`](https://github.com/ganjoor/ganjoor-data) داده‌های گنجور را به‌شکل فایل‌های JSON ساده روی jsDelivr در دسترس قرار می‌دهد و CORS آن هم باز است.

به همین دلیل «دفتر» می‌تواند اطلاعات موردنیازش را مستقیماً از CDN بخواند و همچنان بدون بک‌اند باقی بماند.

فایل `src/lib/ganjoor.ts` با سه نوع فایل سروکار دارد:

* `manifest.json` برای فهرست شاعران
* `_cat.json` برای کتاب‌ها و فصل‌ها
* یک فایل جداگانه برای هر شعر

داده‌های دریافت‌شده هم در حافظه و `sessionStorage` نگه داشته می‌شوند تا لازم نباشد اطلاعات یکسان چندبار دریافت شوند.

گنجور هر بیت را به‌شکل دو مصرع با `CoupletIndex` مشترک ذخیره می‌کند. مصرع‌ها با `Right` برای مصرع اول و `Left` برای مصرع دوم مشخص شده‌اند.

تابع `toCouplets` این ساختار را به مدل بندهای موردنیاز «دفتر» تبدیل می‌کند و مصرع‌های توضیحی با نوع `Comment` را کنار می‌گذارد.

به‌صورت پیش‌فرض، «دفتر» نسخه‌ی `@main` داده‌های گنجور را دنبال می‌کند.

اگر می‌خواهید پروژه همیشه از یک نسخه‌ی مشخص از داده‌ها استفاده کند، می‌توانید آن را روی یک commit ثابت قفل کنید:

```text
VITE_GANJOOR_REF=<commit-sha>
```

> مخزن `ganjoor-data` در حال حاضر مجوز مشخصی اعلام نکرده است. خود شعرهای کلاسیک مدت‌هاست از محدوده‌ی حق‌تکثیر خارج شده‌اند و «دفتر» نیز داده‌ها را هنگام اجرا می‌خواند و نسخه‌ای از آن‌ها را داخل پروژه بازنشر نمی‌کند. بااین‌حال، اگر قصد دارید داده‌های `ganjoor-data` را مستقیماً داخل مخزن خودتان کپی یا بازنشر کنید، بهتر است ابتدا وضعیت مجوز آن را با نگهدارندگان گنجور روشن کنید.

### چرا فایل خروجی دقیقاً شبیه پیش‌نمایش است؟

کتابخانه‌ی `html-to-image` تنها زمانی می‌تواند یک قلم را داخل تصویر نهایی قرار دهد که به فایل آن دسترسی داشته باشد. از طرف دیگر، امکان خواندن مستقیم قواعد یک stylesheet با مبدأ متفاوت (`cross-origin`) را ندارد.

برای حل این مشکل، هر قلم — چه قلم‌هایی که همراه پروژه هستند و چه قلم‌هایی که از Google Fonts می‌آیند — ابتدا دریافت و به `base64` تبدیل می‌شود و بعد هنگام اجرا به‌شکل `@font-face` و با یک `data-URL` در صفحه قرار می‌گیرد.

این منطق در فایل `src/lib/fonts.ts` قرار دارد.

همان CSS از طریق `fontEmbedCSS` به `html-to-image` داده می‌شود. به این ترتیب هم قلم داخل PNG نهایی دقیقاً همان قلم پیش‌نمایش خواهد بود و هم لازم نیست در هر بار گرفتن خروجی، stylesheetها دوباره پیمایش شوند.

خود صفحه هم همیشه با اندازه‌ی منطقی واقعی‌اش در DOM وجود دارد؛ مثلاً یک استوری واقعاً با ابعاد `۱۰۸۰ × ۱۹۲۰` ساخته می‌شود.

برای اینکه این صفحه‌ی بزرگ داخل رابط کاربری جا شود، فقط در پیش‌نمایش با `transform` در CSS کوچک‌تر نمایش داده می‌شود.

بنابراین هنگام گرفتن خروجی، صفحه از ابتدا در اندازه‌ای دیگر دوباره رندر نمی‌شود؛ فایل نهایی همان صفحه‌ی اصلی را با ابعاد واقعی‌اش، پیکسل‌به‌پیکسل ثبت می‌کند.

### پیکربندی

بخش‌های اصلی پروژه را از این فایل‌ها می‌توانید تغییر دهید:

| چه چیزی                                    | کجا                                                            |
| ------------------------------------------ | -------------------------------------------------------------- |
| نام محصول، نشانی، واترمارک و متن‌های فارسی | `src/lib/brand.ts`                                             |
| فهرست قلم‌ها                               | `src/lib/fonts.ts`                                             |
| طرح‌های کاغذ                               | `src/lib/themes.ts`                                            |
| اندازه‌های خروجی                           | `src/lib/formats.ts`                                           |
| نشانی شمارنده                              | `src/lib/counter.ts` یا `VITE_COUNTER_NS` / `VITE_COUNTER_KEY` |
| نسخه‌ی داده‌ی گنجور                        | `VITE_GANJOOR_REF` — پیش‌فرض `main`                            |
| مسیر پایه‌ی Pages                          | `VITE_BASE` — پیش‌فرض `/daftar/`                               |

شمارنده به‌صورت پیش‌فرض از یک سرویس عمومی رایگان استفاده می‌کند.

اگر این سرویس در دسترس نباشد، مسدود شود یا محدودیت درخواست داشته باشد، «دفتر» به‌صورت خودکار سراغ شمارش محلی می‌رود و بقیه‌ی برنامه بدون مشکل به کارش ادامه می‌دهد.

در نتیجه اگر زمانی بخواهید سرویس شمارنده را عوض کنید، تغییر آن فقط به یک فایل محدود می‌شود و روی بخش‌های دیگر برنامه اثری ندارد.

## مجوز قلم‌ها

هر ۱۸ قلمی که در «دفتر» می‌توانید انتخاب کنید، رایگان و دارای مجوز آزاد هستند.

شانزده قلم تحت مجوز **SIL OFL 1.1** منتشر شده‌اند و هنگام اجرا از Google Fonts دریافت می‌شوند؛ بنابراین فایل آن‌ها مستقیماً در این مخزن بازنشر نشده است.

دو قلم داخل [`public/fonts/`](public/fonts/README.md) قرار دارند:

* **میخک** — SIL OFL 1.1
* **تنها** — مجوز Bitstream Vera، با تغییرات نویسنده در مالکیت عمومی

مجوز هر دو قلم ایجاب می‌کند که متن مجوز همراه فایل قلم باقی بماند. به همین دلیل فایل‌های زیر باید در همان پوشه نگه داشته شوند:

```text
Mikhak-OFL.txt
Tanha-LICENSE.txt
```

پیش‌تر ده خانواده‌ی قلم دیگر هم داخل مخزن قرار داشتند، اما حذف شدند؛ چون یا بدون ارائه‌ی مجوز مشخص ادعای حق‌تکثیر داشتند یا تمام حقوق را برای خود محفوظ اعلام کرده بودند.

فهرست این قلم‌ها و دلیل حذف هرکدام در بخش `Removed` فایل `public/fonts/README.md` آمده است.

فهرست قلم‌های قابل انتخاب از آرایه‌ی `FONTS` در `src/lib/fonts.ts` ساخته می‌شود؛ بنابراین اضافه یا حذف کردن یک قلم، تنها به تغییر همان فایل نیاز دارد.

## مجوز

کد «دفتر» تحت مجوز [MIT](LICENSE) منتشر شده است.

یعنی می‌توانید از آن استفاده کنید، fork بگیرید، تغییرش دهید یا حتی در یک پروژه‌ی تجاری به کار ببرید؛ بدون نیاز به اجازه و بدون پرداخت هزینه.

تنها شرط اصلی MIT این است که خط حق‌تکثیر و متن مجوز همراه کد باقی بماند.

اگر دوست داشتید، یک اشاره‌ی ساده به پروژه هم برای ما کافی است؛ مثلاً:

> بر پایه‌ی [دفتر](https://github.com/ParsaBordbar/Daftar)

می‌توانید آن را در README، صفحه‌ی «درباره» یا فوتر پروژه‌تان قرار دهید.

این فقط یک درخواست دوستانه است و بخشی از الزامات مجوز MIT نیست.

مجوز MIT تنها کد و محتوایی را پوشش می‌دهد که متعلق به خود این مخزن است. منابعی که مجوز یا شرایط استفاده‌ی مستقل دارند، همچنان تابع همان شرایط خودشان هستند:

* **قلم‌های داخل مخزن** — میخک با مجوز SIL OFL 1.1 و تنها با مجوز Bitstream Vera. فایل‌های `public/fonts/Mikhak-OFL.txt` و `public/fonts/Tanha-LICENSE.txt` را در هر fork یا build نگه دارید. برای جزئیات بیشتر [`public/fonts/README.md`](public/fonts/README.md) را ببینید.
* **قلم‌های میزبانی‌شده روی گوگل** — تحت SIL OFL 1.1 هستند، هنگام اجرا دریافت می‌شوند و فایل‌هایشان از این مخزن بازنشر نمی‌شود.
* **شعرها** — از مجموعه‌ی گنجور هستند و هنگام اجرا از CDN خوانده می‌شوند. متن شعرهای کلاسیک از محدوده‌ی حق‌تکثیر خارج شده است، اما خود مخزن `ganjoor-data` مجوز مشخصی اعلام نکرده؛ برای جزئیات بیشتر بخش «شعرها از کجا می‌آیند؟» را ببینید.

---

<div align="center">

ساخته‌ی [پارسا بردبار](https://github.com/ParsaBordbar) · شعرها از [گنجور](https://ganjoor.net)

</div>
